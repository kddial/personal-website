<script>
  export let width = false;
  export let height = false;

  if (width) {
    width = `${width}rem`;
  }

  if (height) {
    height = `${height}rem`;
  }
</script>

<div class="space" style:width style:height />
